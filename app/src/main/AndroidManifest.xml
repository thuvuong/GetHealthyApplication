<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.tacoma.uw.css.thuv.gethealthyapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/get_healthy_logo_transparent"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".authenticate.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".food.FoodActivity" />
        <activity android:name=".food.OpenGordonBreakfastActivity" />
        <activity android:name=".food.OpenJamieBreakfast1Activity" />
        <activity android:name=".food.OpenJamieBreakfast2Activity"></activity>
        android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".workout.WorkoutActivity" />
        <activity android:name=".profile.ProfileActivity"></activity>
    </application>

</manifest>